import 'package:flutter/material.dart';

class HomeStart extends StatelessWidget {
  const HomeStart({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Hola mundo"));
  }
}
